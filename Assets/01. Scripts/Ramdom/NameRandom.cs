//Todo: 성별에 따라 주어진 후보군에서 이름을 랜덤으로 가지는 스크립트

using UnityEngine;

public class NameRandom : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
